# 所有文件默认使用 LF 换行符，提交时转换为 LF，检出时不转换
* text eol=lf

# 二进制文件（如图片、可执行文件）不转换换行符
*.png binary
*.jpg binary
*.exe binary
*.zip binary

# 特定文本文件（如 .sh 脚本）强制用 LF
*.sh text eol=lf

# Windows 脚本（.bat/.cmd）可用 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf